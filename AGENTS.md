# Documentation maintenance

Use the public OpenAPI contract and verified product behavior. Never publish credentials, customer records, admin routes, or deployment internals. Keep simulated fixtures and unavailable live capabilities explicit. Update docs.json with every page change. Run npm test and inspect the Mintlify preview before publishing. Do not invent pricing or guaranteed coverage.
